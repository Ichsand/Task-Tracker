package main

const (
	// The name of the file used to store tasks.
	taskFile = "tasks.json"

	// Status constants for tasks.
	StatusTodo       = "todo"
	StatusInProgress = "in-progress"
	StatusDone       = "done"
)
